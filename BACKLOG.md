# BACKLOG — 未確定論点・未解決タスク・不変条件の改訂履歴

CLAUDE.md から移設した運用ログ（2026-08-15）。CLAUDE.md はエージェント全員に毎回
自動読込されるため、毎回の作業に不要な進捗メモ・論点はここに置く。
**このファイルは自動読込されない。** 論点・タスクに着手するときだけ読む。

---

## 不変条件の改訂履歴

改訂したという事実を残すこと自体が目的。元の条文も併記する。

| 日付 | 条文 | 元 | 改訂後 | 根拠 |
|---|---|---|---|---|
| 2026-08-12 | LLM 境界 | 「パースと数値抽出はコードで行い、LLM には**構造化済みの値のみ**渡す」 | **決算情報のクロールと数値抽出は Claude に許可する**。推測での穴埋めも `assumed: true` と根拠の併記を条件に許可する。株価・出来高・信用残のパースは従来どおりコード | steering `decisions.md` **D17**（`sources.yaml` の `never_infer` から `kpi.*` を削除） |
| 2026-08-12 | 約定情報 | D6「`decisions/`（約定価格・数量）はリポジトリに含めない」 | **買値・買付日・数量を `data/master.yaml` の `holding` に載せる**。公開されることを承知のうえでの決定。`decisions/` を `.gitignore` する点は維持 | steering `decisions.md` **D18**（D15 に伴う） |
| 2026-08-12 | bear の隔離 | 「sparse-checkout で `data/` と `src/` のみに制限している」 | `data/prices` `data/margin` `data/indices` `data/kpi` `data/sources.yaml` `src` `bear` を列挙する。`bear/` は成果物を commit するために追加（review-findings F-05）。**`data/master.yaml` は渡さない**（D18 で保有情報が入ったため。F7-1 の目的は保有理由を与えないこと） | review-findings F-05 ＋ D18 との衝突解消 |
| 2026-08-12 | 判定順序 | 「上から順に評価し、該当した時点で確定する」（売りシグナルは③・H5） | **保有銘柄の売りシグナル（雲の下・逆指値抵触・基準到達×デッドクロス気味）は買い側のどのゲートよりも先に評価する**。フェイルセーフの向きは買いと売りで逆 | 敵対的レビュー（売りが上流ゲートに隠れて出ない） |
| 2026-08-23 | 検証状態の明示 | 「`SINGLE_SOURCE` / `MISMATCH` の数値を、そう明示せずに断定形で書かない」を文章（「（参考・未照合）」「株探単独の未照合の参考値」等の都度コピペ）で満たしていた | **記号で明示する**: `✓`=2ソース照合済みの採用値／`※`=未照合・参考値（SINGLE_SOURCE・MISMATCH・出来高・信用残）／`†`=決算短信（一次情報）からの機械抽出。散文では値の直後、表では列見出しか表直前の1行で示し、出典節冒頭に凡例1行を置く。CLAUDE.md 不変条件に「（明示は記号 ※＋凡例でよい）」を追記 | レポート可読性改修（反復・コピペの解消）・マスター指示 2026-08-23 |
| 2026-08-23 | 図の出どころ（D30/D31） | 「手書きは `data:` のみで『手書き（未検証）』が自動表示され、消す方法は用意しない」 | **図型 `diagram`（数値を含まない定性図。ビジネスモデル等の構造説明）を追加**。`origin="diagram"` は「手書き（未検証）」表示の対象外とし、中立 pill（`定性図`）で表示する。ただし label/note に数字が1つでも含まれる diagram は描画を拒否する（D30/D31 の「検証済みデータ由来でない数値を図に出さない」という元の趣旨はそのまま維持し、数値を持たない図だけを対象外にする） | レポート可読性改修（timeline/diagram 図型追加）・マスター指示 2026-08-23 |
| 2026-08-23 | weekly_note の参考値表記 | `build_entry` / `collect_one` が出す注記が「（参考・未照合）」「（参考）」という文章だった | 記号「※」に置換して統一する（出来高・信用残ノートも含む）。過去に生成済みの週次エントリ本文は append-only のため書き換えない | レポート可読性改修（表記の記号化）・マスター指示 2026-08-23 |
| 2026-08-23 | （事実の記録。条文改訂ではない） | 各レポート（150A/3851/4073/4937/6570）の本文（週次以外の4節＋出典）が、検証注記の文章コピペ・円単位の桁羅列・生URL・照合手法の説明表を含む状態だった | 新しい表記規約（記号✓※†・丸め規則・節の責務の分離）に沿って本文を一括清書した。**週次アップデート節はバイト単位で不変**（append-only の対象外にはしていない） | レポート可読性改修・マスター指示 2026-08-23 |

---

## マスターに確認したい論点（コードで決めてよい範囲を超えているもの）

### A. 「週足**中期**移動平均線」は13週か26週か

鉄則の第一条が参照する「中期」の期間が投資ルールに書かれていない。
日本の週足チャートの慣行は 13週=短期 / 26週=中期 / 52週=長期 だが、
鉄則の枠組み全体（6か月2倍ライン）は信用の期限＝26週を軸にしている。

**実データで結論が割れる。** 2026-08-10 時点の 4073 は 13週 flat（+0.207%/週）/
26週 down（-0.334%/週）。13週だけを見ていた旧実装ではこの銘柄だけが
「買いで入ってよい」側に立っていた。

**暫定の扱い**: `data/master.yaml` の `judge.weekly_trend_periods: [13, 26]` で
両方を評価し、どちらかが下向きなら不通過（保守側）。確定したら配列を1つにする。
（経緯は decisions.md M1/D20）

### B. 流動性ゲートを中央値でも引くか

ゲートの根拠は「建てられず降りられない」だが、平均は1日の突出で持ち上がる。
2026-08-10 時点で 4073 は **中央値 470万円**の流動性を平均 6,596万円として通過している
（最大寄与日1日が41.6%）。現状は平均で引き、中央値は台帳に併記するだけ。
（経緯は decisions.md M2）

### C. 「3か月前出来高増加率5倍」の元の定義

楽天証券の定義が未確認で、4通りの解釈を試しても通過4銘柄すべては満たさなかった。
現状は自社定義の値を出し、○×は付けていない（`python src/judge.py` の出力に検証結果が出る。
**台帳のページには出していない**——ここは未開示の穴として残っている）。
（経緯は decisions.md M3/D28）

---

## 未解決タスク

### 1. `master.yaml` の TO_VERIFY 解決（残り5箇所）

2026-08-13 に4件を解消した（3851 決算期・IR URL / 6570 決算期・IR URL）。残りは:

- 3851 日本一ソフトウェア: 業種・ピア
- 6570 共和コーポレーション: 業種・ピア
- 4073: IR URL、ピア2件（DIシステム・シャノンの証券コード）
- 4937: ピア
- 150A JSH: 業種・ピア（2026-08-21 の intake で追加。市場区分・決算期・IR URL は
  一次情報（適時開示PDF・決算短信・決算説明資料）で確認済み）
- 150A JSH: `valuation_model: ev_ebitda` は **Claude の提案でマスター未確認**。
  拠点型で償却が重く、今期が黒字転換の初年度でPERが安定しないこと、
  会社自身がEBITDA倍率で時価総額を語っていることが根拠

**二重照合で確認できたものだけ埋める。確認できないものは TO_VERIFY のまま残す。**

**業種とピアは「1サイトのみ」であることが判明している**（次回の探索を無駄にしないための記録）。
業種（3851 情報・通信業 / 6570 サービス業 / 4937 化学）もピア（株探「比較される銘柄」）も
出所がミンカブ運営の1サイトに閉じており、二重照合が成立しない。
株予報Proの「他社比較」は株探と別の3社を挙げるので、**そもそもピアは出典ごとに違う**。

### 2. KPI（決算）データの投入

`data/kpi/` はまだ空。取り込み手順は `.claude/skills/kabu-ledger/SKILL.md` が正で、
**Claude が一次情報から実額を抽出 → 人間が確認 → commit**（D14/D17）。
比率（前年同期比・1Q進捗率・構成比）は `judge.derive_kpi_metrics()` と
`score.resolve_kpi_metric()` が計算する。**CSV に比率を書かない**（`checks.py` が FAIL）。

KPI が入るまで、ファンダ確認（⑤）は全銘柄で「調査」に落ちる（＝仕様どおり）。

### 3. 分割・権利落ち調整

`checks.py` が整数比の下落を FAIL として検出するところまでは実装済み。
TDnet の一次情報で確認したら `data/corporate_actions.yaml` に
`code / date / kind / ratio / source_url` を記録する（FAIL が WARN に落ちる。
`source_url` が空の記録は「確認していない」として FAIL のまま）。
残るのは調整係数を別列で保持する部分（**生値は書き換えない**）。

### 4. 鉄則のうち未実装の観点

`judge.UNEVALUATED_RULES` が正。`build.py` の `unevaluated_block()` が
**`docs/index.html` の「この台帳が見ていない鉄則」に自動で出す**。
実装したらそこから消すこと。**宣言だけ残さない。**

（2026-08-13 まで CLAUDE.md と README.md は「`docs/formula.html` に自動で出る」と
書いていたが、**そのファイルは一度も生成されていなかった**。`build.py` に
`formula` という文字列が1つも無い。「何を見ていないか」は `judge.py` を
CLI で叩いた人にしか届いておらず、台帳の読者には届いていなかった。）

- 季節性（I-15）— 数年分の日足が要る
- 同業他社の決算（I-16）— `peers` が TO_VERIFY
- 出来高を伴うトレンドか（I-06）
- マクロ（I-18）— CPI・投資部門別売買動向

### 5. `decisions/` への記録（F6-3）

Issue にラベルを付けて閉じたときの記録は未実装。`decisions/` は `.gitignore` 済み。

### 6. 裏取り記録（`data/verification/`）が3銘柄ぶん足りない

| 銘柄 | 状態 | 台帳の表示 |
|---|---|---|
| 3851 | run1 あり。ただし**その後に本文を編集したので sha256 が合わない**（D56） | 「記録が古い」。件数は出さない（D42） |
| 4073 | run1〜2 あり。同じく sha256 が合わない＋取得ログにこの銘柄の行が無い | 同上 |
| 4937 / 6570 | **記録が無い** | 「未検証」 |
| 150A | **記録が無い**（2026-08-21 の intake で初回レポートを作成） | 「未検証」 |

**統合担当・レポートを書いた本人がこれを埋めてはならない**（D55）。
別コンテキストで `.claude/skills/kabu-ledger-verify/SKILL.md` に従って実施する。
埋まるまで WARN が出続けるのは仕様（隠す手段は用意しない）。

### 8. fetch*.py の銘柄指定オプション

intake（新規銘柄の登録）時、`fetch.py --historical` が master.yaml の**全銘柄**の
過去ページを再クロールする（銘柄単位の指定が無い）。既存銘柄ぶんは追記0件で
無害だが、銘柄数×約13秒の無駄。銘柄が増えて intake が遅くなったら
`--code` オプションを追加する（優先度低・現4銘柄では困らない）。

### 7. 決算短信（一次情報）が 4073 と 150A の2件しかない

`data/tanshin/` にあるのは `4073.csv` と `150A.csv` だけ。3851 / 4937 / 6570 は
**一次情報との突き合わせが0件**で、財務数値の裏付けはまとめサイト2社の一致のみ。
各レポートの「未確認・次に確かめること」がその旨を明示している。

`fetch_tanshin.py` は `--date` を渡したときだけ TDnet の当日一覧を見る（省略時は
`sources.yaml` の `known_pdfs` のみ）。150A は `--code 150A --date 2026-08-13` で取れた。
**TDnet の保持は約31日**なので、開示から1か月以内に回さないと `known_pdfs` に
人間が確認したURLを積む経路しか残らない。

とくに **6570 の FY2017-03 経常利益は株探 3,003 / IR BANK 304 と10倍違う**
（`data/fundamentals/6570.csv` で `MISMATCH`）。どちらも採用せず、
レポートに食い違いとして明記してある。**どちらが正しいかは未確認。**

### 9. 1Q の短信は `tanshin_cross_period` を指定できない

`checks._tanshin_cross` は front matter の `tanshin_cross_period` に書いた期キーで
短信（一次情報）とまとめサイト（二次情報）を突き合わせる。ところが 150A の1Q短信では:

- 短信側の期は `FY2027Q1cum`（1Q累計）
- `data/fundamentals/150A.csv` 側は `Q2026-04_2026-06`（単独3か月）しか持たない
- `Period.matches` はこの2つを一致とみなさない。`C2026-04_2026-06` なら一致するが、
  そのキーは fundamentals に存在しない（まとめサイトが1Qを累計として出さないため）

結果、どちらを書いても突合0件になる。**1Qは累計＝当該四半期なので中身は同じ**であり、
目視では 売上1,466／営業利益105／経常102 が短信とまとめサイトで一致している。
仕組みとしては、①1Q に限り単独四半期キーを累計とみなす、②`fetch_fundamentals` が
1Q に `C` キーも書く、のどちらかが要る。**現状は「突合していない」という WARN が
出続けるのが正しい表示**（一致していると機械が言ったわけではない）。

### 10. front matter を1文字変えるだけで裏取り記録が「記録が古い」になる

`verification.report_sha256` は `reports/{code}.md` を**丸ごと**ハッシュする
（`src/verification.py`）。そのため本文の主張が1文字も変わっていなくても、
front matter の `deep_dive` を切り替えただけで sha256 がずれ、
台帳の表示が「37/54件」から「**記録が古い**」に落ちる。

2026-08-21 に実際に起きた（「深掘り中」の表示名変更に合わせて 4073 の
`deep_dive: true → false` を下ろしたところ、4073 の裏取り件数が非表示になった）。

- **いまの挙動は間違いではない**。記録が現在の本文を保証しないのは事実であり、
  D42/D56 の「疑わしいときは件数を出さない」に沿っている
- ただし**検証対象は本文の主張であって front matter ではない**ので、
  ハッシュを本文（`---` 以降）だけに限る余地はある
- 変えると D56 の契約（run が持つ sha256 の意味）が変わる。**マスターの判断が要る。**
  変えない場合は、フラグを触ったら裏取りを回し直す運用になる
